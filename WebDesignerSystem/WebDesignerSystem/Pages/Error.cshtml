@page
@model ErrorModel
@{
    ViewData["Title"] = "Ошибка";
}

<div class="error-page">
    <div class="container text-center py-5">
        <div class="mb-4">
            <i class="bi bi-exclamation-triangle display-1 text-danger"></i>
        </div>
        <h1 class="display-4 fw-bold text-danger mb-3">Ошибка</h1>
        <p class="lead mb-4">Произошла ошибка при обработке вашего запроса</p>
        
        @if (Model.ShowRequestId)
        {
            <div class="card mb-4 mx-auto" style="max-width: 500px;">
                <div class="card-body">
                    <p class="mb-2 text-muted">ID запроса:</p>
                    <code class="bg-light p-2 rounded d-block">@Model.RequestId</code>
                </div>
            </div>
        }
        
        <div class="d-flex flex-wrap justify-content-center gap-3">
            <a asp-page="/Index" class="btn btn-primary">
                <i class="bi bi-house me-2"></i>На главную
            </a>
            <button onclick="window.history.back()" class="btn btn-outline-primary">
                <i class="bi bi-arrow-left me-2"></i>Назад
            </button>
        </div>
        
        <!-- Информация о режиме разработки (статическая информация) -->
        <div class="mt-5 text-start">
            <div class="card">
                <div class="card-header bg-warning">
                    <h5 class="mb-0">Информация о режиме разработки</h5>
                </div>
                <div class="card-body">
                    <p>
                        Переключение в <strong>Development</strong> среду отображает подробную информацию об ошибке.
                    </p>
                    <p class="mb-0">
                        <strong>Development среда не должна быть включена для развернутых приложений.</strong>
                        Это может привести к отображению конфиденциальной информации конечным пользователям.
                        Для локальной отладки включите <strong>Development</strong> среду, установив переменную окружения 
                        <strong>ASPNETCORE_ENVIRONMENT</strong> в <strong>Development</strong> и перезапустив приложение.
                    </p>
                </div>
            </div>
        </div>
    </div>
</div>

<style>
    .error-page {
        min-height: 70vh;
        display: flex;
        align-items: center;
    }
</style>